server:
  port: 8081

spring:
  application:
    name: reto-aws

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/reto_aws
    username: dummyValue
    password: dummyValue

  springdoc:
    api-docs:
      path: /v3/api-docs
    swagger-ui:
      path: /swagger-ui.html

logging:
  level:
    root: INFO
    org.springframework.r2dbc: DEBUG
    io.r2dbc.postgresql: INFO